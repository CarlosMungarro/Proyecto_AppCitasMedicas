<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFF0F0" /> <!-- Fondo más claro al presionar -->
            <stroke android:width="2dp" android:color="@color/rojoPrincipal" />
            <corners android:radius="12dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" /> <!-- Fondo blanco -->
            <stroke android:width="2dp" android:color="@color/rojoPrincipal" /> <!-- Borde rojo -->
            <corners android:radius="12dp" />
        </shape>
    </item>
</selector>

